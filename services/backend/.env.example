# Comma-separated list of allowed CORS origins.
# Add your Supabase project URL and any front-end origins here.
ALLOWED_ORIGINS=http://localhost:3000,https://your-supabase-project.supabase.co,http://localhost:5678

# Secret key used to validate requests arriving from n8n webhooks.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
N8N_SECRET_KEY=your-secret-here

# Python logging level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# Set to "development" to enable uvicorn auto-reload (never use in production)
ENV=production

# Uvicorn bind port (Dockerfile default: 8001)
PORT=8001
