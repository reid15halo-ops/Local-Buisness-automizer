# HandwerkFlow Email Relay - Konfiguration
# Kopiere diese Datei zu .env und passe die Werte an

# API Secret (generiere mit: openssl rand -hex 32)
API_SECRET=dein-geheimer-api-schluessel

# Server Port
PORT=3100

# Proton Mail Bridge SMTP Einstellungen
# (Bridge muss auf dem selben Server laufen)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=deine-email@protonmail.com
SMTP_PASS=bridge-generiertes-passwort

# Absender
SENDER_NAME=HandwerkFlow
SENDER_EMAIL=deine-email@protonmail.com
