version: "3.8"

services:
  email-relay:
    build: .
    container_name: freyavision-email
    restart: unless-stopped
    ports:
      - "3100:3100"   # Outbound HTTP API
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
